# .readthedocs.yml

version: 2
build:
  os: ubuntu-20.04
  tools:
    python: mambaforge-4.10

# Build PDF in addition to default HTML and JSON
formats:
    - pdf

conda:
  environment: environment.yml
