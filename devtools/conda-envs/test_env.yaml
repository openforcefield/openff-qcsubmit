name: basic_qcsubmit

channels:
  - conda-forge
  - omnia
  - defaults

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - openforcefield
  - openforcefields
  - smirnoff99Frosst
  - py-cpuinfo <= 5 # remove pin when qcengine is fixed
  - pyyaml
  - codecov
  - qcportal
  - openeye-toolkits
  - qcfractal
  - torsiondrive
  - fragmenter

    # Pip-only installs
  - pip:
    - basis_set_exchange