name: test

channels:
  - conda-forge
  - omnia
  - defaults

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - cmiles
  - fragmenter
  - openeye-toolkits
  - qcportal

    # Pip-only installs
  #- pip:
  #  - codecov
