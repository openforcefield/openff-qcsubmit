name: test

channels:
  - conda-forge
  - omnia
  - defaults

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - openforcefield
  - openforcefields
  - smirnoff99Frosst
  - pyyaml
  - codecov
  - fragmenter
  - qcportal
  - qcengine

    # Pip-only installs
#  - pip:
#    - torchani
