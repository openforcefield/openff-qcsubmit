name: basic_qcsubmit

channels:
  - conda-forge
  - omnia
  - defaults

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - openforcefield
  - openforcefields
  - smirnoff99Frosst
  - pyyaml
  - codecov
  - qcportal
  - openeye-toolkits

    # Pip-only installs
  - pip:
    - basis_set_exchange