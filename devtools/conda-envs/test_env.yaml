name: basic_qcsubmit

channels:
  - conda-forge
  - omnia
  - defaults

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - openforcefield >=0.7.0
  - openforcefields
  - smirnoff99Frosst
  - openmmforcefields
  - qcengine >=0.15.0
  - pyyaml
  - codecov
  - qcportal
  - openeye-toolkits
  - qcfractal
  - torsiondrive
  - fragmenter

    # Pip-only installs
  - pip:
    - basis_set_exchange